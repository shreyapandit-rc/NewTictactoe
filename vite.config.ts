import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/NewTictactoe/', // required for GitHub Pages (replace with your repo name if different)
})
